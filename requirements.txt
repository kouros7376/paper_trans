# ============================================================
# 다우오피스 전자결재 기안 자동화 스크립트 필요 패키지
# ============================================================
# 설치 방법 (PowerShell 또는 명령 프롬프트에서 실행):
#   pip install -r requirements.txt
# ============================================================

# HTTP 요청 라이브러리 — API 호출에 사용
requests>=2.31.0

# .env 파일에서 환경 변수를 자동으로 로드해주는 도구
python-dotenv>=1.0.0
